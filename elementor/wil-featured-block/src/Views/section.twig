{% set componentName = "wil-featured-block" %}

{% include "Containers/" ~ data.general.variant ~ ".twig" with {
  content: data.general.content,
  titleFont: data.general.titleFont,
  textFont: data.general.textFont,
  responsive: data.responsive,
  carousel: data.carousel,
} %}
